name: Deploy site

# Publishes website/ to Cloudflare Pages. Zero-build static folder — no install,
# no bundler — so this is a plain upload.
#
# Setup: one repo secret, CLOUDFLARE_API_TOKEN, with the "Cloudflare Pages — Edit"
# permission. No account ID needed: wrangler resolves it from the token as long as
# the token maps to a single account (same as heapcode's site deploy).

on:
  # ordinary content edits
  push:
    branches: [main]
    paths:
      - 'website/**'
      - '.github/workflows/deploy-site.yml'

  # Publishing a release is the moment /releases/latest starts resolving to the new
  # version, which is what the site's download buttons point at. Redeploying here keeps
  # the two in lockstep: without it, publishing a release leaves the live site pointing
  # at the previous version's filenames, which no longer exist in the newest release —
  # so every download button 404s until someone remembers to redeploy by hand.
  release:
    types: [published]

  # lets a stale site be redeployed without an empty commit
  workflow_dispatch:

# One deploy at a time. cancel-in-progress stays false deliberately: an upload already
# talking to Cloudflare should finish rather than leave a half-published state.
concurrency:
  group: deploy-site
  cancel-in-progress: false

jobs:
  deploy:
    name: Cloudflare Pages
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5

      # No `if: secrets... != ''` guard, matching heapcode's reasoning: a guard that
      # SKIPS on a missing token makes the run look green while nothing deployed. A
      # missing token should fail loudly here instead.
      #
      # --branch is what marks this a production deploy rather than a preview; it has
      # to match the Pages project's configured production branch.
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy website --project-name=heapchat --branch=main
